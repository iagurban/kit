# Function: interleaveWithObject()

```ts
function interleaveWithObject<T, V>(a, o): (T | V)[];
```

Defined in: [interleave-with-object.ts:10](https://github.com/iagurban/kit/blob/8b774e0e19a5bd4b27ec02cbd39cc62b919e327f/src/react/interleave-with-object.ts#L10)

Interleaves elements of an array with additional elements generated by a provided function.

## Type Parameters

### T

`T`

The type of the elements in the input array.

### V

`V`

The type of the elements generated by the function.

## Parameters

### a

`T`[]

The input array to be interleaved.

### o

(`prev`, `i`) => `V`

A function that generates a new element to interleave. It receives the previous element (either from the array or previously generated) and its index in the process.

## Returns

(`T` \| `V`)[]

A new array where elements from the input array are interleaved with the elements generated by the function.
