# Function: isObjectEmpty()

```ts
function isObjectEmpty(o): boolean;
```

Defined in: [IdeaProjects/kit/kit/src/core/collections/object-utils.ts:111](https://github.com/iagurban/kit/blob/1e781a5487ee363602a36fe6fd9858d348e2b67a/src/core/collections/object-utils.ts#L111)

Checks whether the given object is empty (i.e., contains no own enumerable properties).

## Parameters

### o

`object`

The object to be checked.

## Returns

`boolean`

Returns `true` if the object is empty, otherwise `false`.
