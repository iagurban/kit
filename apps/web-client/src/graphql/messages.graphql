subscription AllMessagesSubscription {
  message: messagesSubscription {
    nn
  }
}

query JoinedChats {
  joinedChats {
    id
    title
    bio
    userPermissions {
      permissions
      role {
        permissions
        tags
      }
    }
  }
}
