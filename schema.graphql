schema
  @link(url: "https://specs.apollo.dev/link/v1.0")
  @link(url: "https://specs.apollo.dev/join/v0.3", for: EXECUTION)
{
  query: Query
  mutation: Mutation
  subscription: Subscription
}

directive @join__enumValue(graph: join__Graph!) repeatable on ENUM_VALUE

directive @join__field(graph: join__Graph, requires: join__FieldSet, provides: join__FieldSet, type: String, external: Boolean, override: String, usedOverridden: Boolean) repeatable on FIELD_DEFINITION | INPUT_FIELD_DEFINITION

directive @join__graph(name: String!, url: String!) on ENUM_VALUE

directive @join__implements(graph: join__Graph!, interface: String!) repeatable on OBJECT | INTERFACE

directive @join__type(graph: join__Graph!, key: join__FieldSet, extension: Boolean! = false, resolvable: Boolean! = true, isInterfaceObject: Boolean! = false) repeatable on OBJECT | INTERFACE | UNION | ENUM | INPUT_OBJECT | SCALAR

directive @join__unionMember(graph: join__Graph!, member: String!) repeatable on UNION

directive @link(url: String, as: String, for: link__Purpose, import: [link__Import]) repeatable on SCHEMA

type AttachmentInfo
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)
{
  fileId: ID!
  filename: String!
  metadata: JSON
  mimeType: String!
  size: BigInt!
  url: String!
}

type AttachmentPreview
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)
{
  thumbnail: String
  type: String!
}

"""64-bit signed integer"""
scalar BigInt
  @join__type(graph: CHATS)
  @join__type(graph: FILES)
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)

type Chat
  @join__type(graph: CHATS)
{
  _count: ChatCount!
  avatar: String
  bio: String
  defaultRole: ChatRole
  defaultRoleId: String
  events: [ChatEvent!]
  eventsCounter: ChatEventsCounter
  id: ID!
  members: [ChatMember!]
  messagesCounter: MessagesCounter
  owner: User!
  ownerId: String!
  roles: [ChatRole!]
  title: String!
  userPermissions: [UserChatPermissions!]
}

type ChatCount
  @join__type(graph: CHATS)
{
  events: Int!
  members: Int!
  roles: Int!
  userPermissions: Int!
}

type ChatEvent
  @join__type(graph: CHATS)
{
  author: User!
  authorId: String!
  chat: Chat!
  chatId: String!
  createdAt: DateTime!
  id: ID!
  nn: String!
  payload: JSON!
  type: String!
}

type ChatEventsCounter
  @join__type(graph: CHATS)
{
  chat: Chat!
  chatId: ID!
  lastNn: String!
}

type ChatMember
  @join__type(graph: CHATS)
{
  chat: Chat!
  chatId: String!
  joinedAt: DateTime!
  user: User!
  userId: String!
}

type ChatRole
  @join__type(graph: CHATS)
{
  _count: ChatRoleCount!
  chat: Chat!
  chatId: String!
  id: ID!
  isDefaultForChats: [Chat!]
  name: String!
  permissions: JSON!
  tags: [ChatRoleTag!]
  userPermissions: [UserChatPermissions!]
}

type ChatRoleCount
  @join__type(graph: CHATS)
{
  isDefaultForChats: Int!
  userPermissions: Int!
}

enum ChatRoleTag
  @join__type(graph: CHATS)
{
  ADMIN @join__enumValue(graph: CHATS)
  BANNED @join__enumValue(graph: CHATS)
  MEMBER @join__enumValue(graph: CHATS)
  MODERATOR @join__enumValue(graph: CHATS)
  VIEWER @join__enumValue(graph: CHATS)
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime
  @join__type(graph: CHATS)
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)

type ForwardInfo
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)
{
  attachments: [AttachmentInfo!]
  authorId: ID!
  chatId: ID!
  createdAt: DateTime!
  nn: BigInt!
  text: String
}

type GetLinkResponse
  @join__type(graph: FILES)
{
  reportToken: String!
  upload: String!
}

type GuideFailed
  @join__type(graph: FILES)
{
  error: String!
}

type GuideFile
  @join__type(graph: FILES)
{
  file: UploadedFile!
}

union GuideResponse
  @join__type(graph: FILES)
  @join__unionMember(graph: FILES, member: "GuideFailed")
  @join__unionMember(graph: FILES, member: "GuideFile")
  @join__unionMember(graph: FILES, member: "GuideUpload")
 = GuideFailed | GuideFile | GuideUpload

type GuideUpload
  @join__type(graph: FILES)
{
  uploads: [UploadPart!]!
}

scalar join__FieldSet

enum join__Graph {
  CHATS @join__graph(name: "chats", url: "http://localhost:11112/graphql")
  FILES @join__graph(name: "files", url: "http://localhost:9999/graphql")
  MESSAGES @join__graph(name: "messages", url: "http://localhost:22225/graphql")
  SUBSCRIPTIONS @join__graph(name: "subscriptions", url: "http://localhost:3335/graphql")
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON
  @join__type(graph: CHATS)
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)

scalar link__Import

enum link__Purpose {
  """
  `SECURITY` features provide metadata necessary to securely resolve fields.
  """
  SECURITY

  """
  `EXECUTION` features provide metadata necessary for operation execution.
  """
  EXECUTION
}

type Message
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)
{
  attachments: [AttachmentInfo!]
  authorId: ID!
  chatId: ID!
  createdAt: DateTime!
  deletedAt: DateTime
  editedAt: DateTime
  editedNn: BigInt
  eventNn: BigInt
  forwarded: [ForwardInfo!]
  nn: BigInt!
  replyToNn: BigInt
  replyToPreview: ReplyToPreview
  text: String
  updatedAt: DateTime!
}

type MessagesCounter
  @join__type(graph: CHATS)
{
  chat: Chat!
  chatId: ID!
  lastNn: String!
}

type Mutation
  @join__type(graph: CHATS)
  @join__type(graph: FILES)
{
  pushChatEvent(chatId: ID!, payload: JSON!, type: String!): PushEventResponseDto! @join__field(graph: CHATS)
  createOrJoinUploadSession(checksum: String!, mimeType: String!, originalFilename: String!, parallel: Int, sizeBytes: BigInt!, totalChunks: Int!): GuideResponse! @join__field(graph: FILES)
  reportChunk(body: ReportChunkInput!, parallel: Int, reportToken: String!): GuideResponse! @join__field(graph: FILES)
}

type PushEventResponseDto
  @join__type(graph: CHATS)
{
  """Серверное время генерации события"""
  createdAt: DateTime!

  """Присвоенный порядковый номер события в чате"""
  nn: String!
}

type Query
  @join__type(graph: CHATS)
  @join__type(graph: FILES)
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)
{
  dummyQuery: Float! @join__field(graph: CHATS)
  joinedChats: [Chat!]! @join__field(graph: CHATS)
  joinedChatsIds: [String!]! @join__field(graph: CHATS)
  getUrlForChunk(checksumSha256: String!, chunkId: String!): GetLinkResponse! @join__field(graph: FILES)
  getMessages(
    chatId: String!

    """Greater than. Fetches messages newer than this nn."""
    gt: BigInt
    limit: Int = 30

    """Less than. Fetches messages older than this nn."""
    lt: BigInt
  ): [Message!]! @join__field(graph: MESSAGES)
}

type RefreshToken
  @join__type(graph: CHATS)
{
  createdAt: DateTime!
  expiresAt: DateTime!
  hash: String!
  id: ID!
  user: User!
  userId: String!
}

type ReplyToPreview
  @join__type(graph: MESSAGES)
  @join__type(graph: SUBSCRIPTIONS)
{
  attachments: [AttachmentPreview!]
  authorId: ID!
  nn: ID!
  text: String
}

input ReportChunkInput
  @join__type(graph: FILES)
{
  eTag: String
  message: String
  status: Int
}

type StoredFile
  @join__type(graph: CHATS)
{
  cdnUrl: String!
  checksum: String!
  createdAt: DateTime!
  id: ID!
  metadata: JSON
  mimeType: String!
  originalFilename: String!
  sizeBytes: String!
  storageKey: String!
  updatedAt: DateTime!
  uploadSession: UploadSession
  uploadedByUser: User!
  uploadedByUserId: String!
}

type Subscription
  @join__type(graph: SUBSCRIPTIONS)
{
  messagesSubscription: Message!
}

type UploadChunk
  @join__type(graph: CHATS)
{
  eTag: String
  id: ID!
  leasedAt: DateTime
  partNumber: Int!
  session: UploadSession!
  sessionId: String!
}

type UploadedFile
  @join__type(graph: FILES)
{
  cdnUrl: String!
  id: String!
}

type UploadPart
  @join__type(graph: FILES)
{
  chunkId: String!
  partNumber: Int!
}

type UploadSession
  @join__type(graph: CHATS)
{
  _count: UploadSessionCount!
  chunks: [UploadChunk!]
  createdAt: DateTime!
  failReason: String
  file: StoredFile!
  fileId: String!
  id: ID!
  status: UploadStatus!
  storageUploadId: String!
  totalChunks: Int!
  totalFailureCount: Int!
}

type UploadSessionCount
  @join__type(graph: CHATS)
{
  chunks: Int!
}

enum UploadStatus
  @join__type(graph: CHATS)
{
  ACTIVE @join__enumValue(graph: CHATS)
  FAILED @join__enumValue(graph: CHATS)
  FINALIZING @join__enumValue(graph: CHATS)
}

type User
  @join__type(graph: CHATS)
{
  RefreshToken: [RefreshToken!]
  _count: UserCount!
  abbrev: String
  chatEvents: [ChatEvent!]
  chatsMmbership: [ChatMember!]
  chatsPermissions: [UserChatPermissions!]
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String!
  ownChats: [Chat!]
  uploadedFiles: [StoredFile!]
}

type UserChatPermissions
  @join__type(graph: CHATS)
{
  chat: Chat!
  chatId: String!
  permissions: JSON
  role: ChatRole
  roleId: String
  user: User!
  userId: String!
}

type UserCount
  @join__type(graph: CHATS)
{
  RefreshToken: Int!
  chatEvents: Int!
  chatsMmbership: Int!
  chatsPermissions: Int!
  ownChats: Int!
  uploadedFiles: Int!
}