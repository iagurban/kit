{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Poslah Nodemon Executor",
  "description": "Runs a script using nodemon, based on its CLI options.",
  "type": "object",
  "properties": {
    "script": {
      "type": "string",
      "description": "The script to run if 'exec' is not specified (e.g., 'server.js')."
    },
    "exec": {
      "type": "string",
      "description": "The command to execute (e.g., 'ts-node app.ts'). Takes precedence over 'script'."
    },
    "scriptArgs": {
      "type": "array",
      "description": "Arguments to pass to the script itself.",
      "items": {
        "type": "string"
      }
    },
    "watch": {
      "description": "Globs to watch for changes, or a false value to disable watching, run the process once and exit.",
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "ignore": {
      "type": "array",
      "description": "File patterns or directories to ignore.",
      "items": {
        "type": "string"
      }
    },
    "ext": {
      "type": "string",
      "description": "Comma-separated list of file extensions to watch (e.g., 'js,pug,hbs')."
    },
    "verbose": {
      "type": "boolean",
      "description": "Show detail on what is causing restarts.",
      "default": false
    },
    "cwd": {
      "type": "string",
      "description": "The working directory to run nodemon from. It will be created if it doesn't exist."
    },
    "runner": {
      "type": "string",
      "description": "The package runner to use for executing the command (e.g., 'yarn', 'npx', 'pnpm').",
      "default": "yarn"
    },
    "delay": {
      "type": "number",
      "description": "Delay in ms before restarting the process after a file change."
    },
    "signal": {
      "type": "string",
      "description": "The signal to send to the process when killing it (e.g., 'SIGTERM')."
    },
    "logChangedFiles": {
      "type": "boolean",
      "description": "Log the files that changed, triggering a restart."
    },
    "logTrackedFiles": {
      "type": "boolean",
      "description": "Log all the files and directories being watched."
    },
    "filesCheckInterval": {
      "type": "number",
      "description": "How often to check for file changes in milliseconds."
    },
    "dirsCheckInterval": {
      "type": "number",
      "description": "How often to check for directory changes in milliseconds."
    },
    "spawnOptions": {
      "type": "object",
      "description": "Spawn options for the child process, as a JSON object."
    }
  },
  "required": ["watch"],
  "anyOf": [
    { "required": ["script"] },
    { "required": ["exec"] }
  ]
}
